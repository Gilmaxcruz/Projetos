@model Mantimentos.App.ViewModels.MarcaViewModel


@{
    ViewData["Title"] = "Detalhes";
}

<h1>@ViewData["Title"]</h1>

<div>
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Nome)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Nome)
        </dd>
    </dl>
</div>
<div>
    <a class="btn btn-warning text-white" asp-action="Edit" asp-route-id="@Model.Id"><i class="fas fa-edit"></i></a>
    <a class="btn btn-info" asp-action="Index"><i class="fas fa-chevron-left"></i></a>
</div>
